# Git 日常更新推送指南

## 📝 日常开发流程（最常用）

当您修改了代码后，使用以下三步命令推送到 Git：

```powershell
# 1. 添加所有修改的文件
git add .

# 2. 提交修改（附带说明）
git commit -m "描述你做了什么修改"

# 3. 推送到远程仓库
git push
```

就这么简单！**记住这三条命令就够了**。

## 🎯 实际例子

### 例子 1：修复了一个 Bug

```powershell
cd d:\pear_admin\pear-admin-flask

git add .
git commit -m "修复：订单列表分页显示错误"
git push
```

### 例子 2：添加了新功能

```powershell
git add .
git commit -m "新增：供应商管理模块"
git push
```

### 例子 3：更新了文档

```powershell
git add .
git commit -m "文档：更新 README 部署说明"
git push
```

### 例子 4：优化了性能

```powershell
git add .
git commit -m "优化：数据库查询性能提升"
git push
```

## 📋 推送前的检查流程（建议）

```powershell
# 1. 查看修改了哪些文件
git status

# 2. 查看具体修改内容（可选）
git diff

# 3. 添加文件
git add .

# 4. 提交
git commit -m "你的修改说明"

# 5. 推送
git push
```

## 💡 Commit 消息规范（建议遵循）

好的 commit 消息能让你更容易追踪修改历史：

```
格式：类型：简短描述

常用类型：
- 新增：添加新功能
- 修复：修复 Bug
- 优化：性能优化或代码改进
- 文档：文档更新
- 样式：UI/CSS 样式调整
- 重构：代码重构（不改变功能）
- 测试：添加或修改测试
- 配置：配置文件修改
```

**示例：**
```powershell
git commit -m "新增：用户头像上传功能"
git commit -m "修复：登录验证码显示异常"
git commit -m "优化：首页加载速度提升 50%"
git commit -m "文档：添加 API 接口说明"
```

## 🔄 其他常用操作

### 查看提交历史

```powershell
# 查看详细历史
git log

# 查看简洁历史
git log --oneline

# 查看最近 5 条记录
git log -5

# 查看某个文件的修改历史
git log -- 文件路径
```

### 撤销修改

```powershell
# 撤销工作区的修改（还未 add）
git checkout -- 文件名

# 撤销暂存区的修改（已经 add，但未 commit）
git reset HEAD 文件名

# 撤销上一次提交（已经 commit，但未 push）
git reset --soft HEAD^
```

### 拉取最新代码

如果有多人协作或在不同电脑上开发：

```powershell
# 拉取并合并远程最新代码
git pull

# 或者分步执行
git fetch origin
git merge origin/main
```

## ⚠️ 常见问题

### 问题 1：推送时提示需要先拉取

```
error: failed to push some refs
hint: Updates were rejected because the remote contains work that you do not have locally
```

**解决：**
```powershell
# 先拉取远程代码
git pull

# 如果有冲突，解决冲突后
git add .
git commit -m "合并远程更改"

# 再推送
git push
```

### 问题 2：不小心提交了敏感文件

```powershell
# 从 Git 中删除文件（但保留本地文件）
git rm --cached 文件名

# 添加到 .gitignore
echo "文件名" >> .gitignore

# 提交删除操作
git add .
git commit -m "删除敏感文件"
git push
```

### 问题 3：忘记写 commit 消息

```powershell
# 修改上一次的 commit 消息
git commit --amend -m "新的消息"

# 如果已经推送，需要强制推送（谨慎使用）
git push -f
```

### 问题 4：只想提交部分文件

```powershell
# 添加指定文件
git add 文件1.py 文件2.py

# 或者添加某个目录
git add pear_admin/apis/

# 提交
git commit -m "更新指定文件"
git push
```

## 🚀 快速参考卡片

### 最常用的命令（90% 的情况）

```powershell
git status          # 查看状态
git add .           # 添加所有修改
git commit -m "说明"  # 提交修改
git push            # 推送到远程
git pull            # 拉取远程代码
```

### 查看类命令

```powershell
git status          # 查看工作区状态
git log             # 查看提交历史
git log --oneline   # 简洁历史
git diff            # 查看修改内容
git branch          # 查看分支
git remote -v       # 查看远程仓库
```

### 撤销类命令

```powershell
git checkout -- 文件   # 撤销工作区修改
git reset HEAD 文件    # 取消暂存
git reset --soft HEAD^ # 撤销上次提交
```

## 📱 完整工作流程示例

```powershell
# 早上开始工作
cd d:\pear_admin\pear-admin-flask
git pull  # 拉取最新代码

# ... 修改代码 ...

# 完成一个功能后
git status  # 查看修改了什么
git add .
git commit -m "新增：项目统计图表功能"
git push

# ... 继续修改 ...

# 完成另一个功能
git add .
git commit -m "优化：订单列表查询速度"
git push

# 下班前
git status  # 确保所有修改都已提交
```

## 🎓 进阶技巧

### 使用分支开发新功能

```powershell
# 创建并切换到新分支
git checkout -b feature/new-feature

# 开发并提交
git add .
git commit -m "开发新功能"

# 推送新分支
git push -u origin feature/new-feature

# 开发完成后，切回主分支
git checkout main

# 合并新功能
git merge feature/new-feature

# 推送主分支
git push
```

### 暂存当前工作

```powershell
# 临时保存当前修改
git stash

# 切换分支或拉取代码
git pull

# 恢复之前的修改
git stash pop
```

## 📌 记住这个流程

```
修改代码 → git add . → git commit -m "说明" → git push
```

**就是这么简单！** 🎉

## 🔗 相关文档

- 完整 Git 指南：[GIT_GUIDE.md](./GIT_GUIDE.md)
- 部署指南：[DEPLOY.md](./DEPLOY.md)
- 项目说明：[README.md](./README.md)

---

**提示**：把这个文档保存到收藏夹，需要时随时查阅！
